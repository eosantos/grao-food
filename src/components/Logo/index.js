import React from "react";
import logo from "../../assets/icon/logo.png";

const Logo = () => {
  return (
    <div>
      <img height={60} alt="logo" src={logo} />
    </div>
  );
};

export default Logo;
